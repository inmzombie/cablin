{% extends 'MWSimpleCrudGeneratorBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - {{ 'views.show.show'|trans({'%entity%': 'Cliente'}, 'MWSimpleCrudGeneratorBundle') }}
{% endblock %}

{% block page %}
    <h1>Cliente</h1>

    <form class="form-horizontal">
        <fieldset>                <div class="control-group">
                    <label class="control-label">Id</label>
                    <div class="controls">                        <span class="input-xlarge uneditable-input">{{ entity.id }}</span>                    </div>
                </div>                <div class="control-group">
                    <label class="control-label">Numerocliente</label>
                    <div class="controls">                        <span class="input-xlarge uneditable-input">{{ entity.numeroCliente }}</span>                    </div>
                </div>                <div class="control-group">
                    <label class="control-label">Nombreapellido</label>
                    <div class="controls">                        <span class="input-xlarge uneditable-input">{{ entity.nombreApellido }}</span>                    </div>
                </div>                <div class="control-group">
                    <label class="control-label">Documento</label>
                    <div class="controls">                        <span class="input-xlarge uneditable-input">{{ entity.documento }}</span>                    </div>
                </div>                <div class="control-group">
                    <label class="control-label">Telefono</label>
                    <div class="controls">                        <span class="input-xlarge uneditable-input">{{ entity.telefono }}</span>                    </div>
                </div>        </fieldset>
    </form>

        <div>
    <div class="float-left">
        <a class="btn" href="{{ path('admin_cliente') }}">
            {{ 'views.recordactions.backtothelist'|trans({}, 'MWSimpleCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">
        <a class="btn btn-primary" href="{{ path('admin_cliente_edit', { 'id': entity.id }) }}">
            {{ 'views.recordactions.edit'|trans({}, 'MWSimpleCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left">{{ form(delete_form) }}</div>
</div>
{% endblock %}
